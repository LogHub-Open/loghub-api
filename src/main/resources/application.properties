spring.application.name=loghub-api

# Default profile
spring.profiles.active=dev

# API Key configuration
loghub.api.key=${LOGHUB_API_KEY:loghub-dev-key-2024}

# JPA settings
spring.jpa.open-in-view=false

